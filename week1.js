        function calculate(){
            var input = $('#expression'); //get the box
            var val = input.val(); //get value inside the box
            console.log(val.toUpperCase()); //log it
            } 